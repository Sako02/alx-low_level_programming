#include "main.h"
/**
 * print_sign - a function that prints the sign of a number.
 * @n: function parameters
 *  * Return: Always 0.
 */
int _abs(int N)
{
if (N < 0)
{
N = (-1) * N;
}
_putchar(N);
return (0);
}
